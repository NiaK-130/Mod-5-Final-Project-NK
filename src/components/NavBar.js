import React from 'react'
import { NavLink } from 'react-router-dom'

function NavBar() {

    return (
        <div>
            {/* <NavLink 
                className="nav-link" 
                exact to="/home">
                Home
            </NavLink>

            <NavLink 
                className="nav-link"
                exact to="/dashboard">
                Dashboard
            </NavLink>

            <NavLink 
                className="nav-link"
                exact to="/profile">
                Profile
            </NavLink> */}
        </div>
    )
}

export default NavBar